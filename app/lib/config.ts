const Config = {
  yapily: {
    key: process.env.YAPILY_KEY,
    secret: process.env.YAPILY_SECRET,
    api: process.env.YAPILY_API,
    applicationUserId: process.env.YAPILY_APP_USER_ID,
  },
};

export default Config;
